name: Deploy to Render
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        run: curl <https://api.render.com/deploy/srv-d49amt3e5dus73ckomk0?key=ySSlCcNCS8c>
